__pycache__/
*.pyc
# Artefactos del scorer: los DOS ficheros concretos (planes CP-SAT + modelo
# LightGBM) SÍ se versionan — son la fuente de verdad del behavioural cloning
# y tardan ~4h en regenerarse. El resto de la carpeta es regenerable y se ignora.
#
# whitelist:
!labels/planes.jsonl
!labels/.gitkeep
!models/scorer_full.txt
!models/.gitkeep
